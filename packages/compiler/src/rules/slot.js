import {
	memberExpression,
	identifier as id,
	arrowFunctionExpression,
	blockStatement,
	callExpression,
} from "@babel/types";

export default function slot(context, options)
{
	// console.warn(this.tag);
}